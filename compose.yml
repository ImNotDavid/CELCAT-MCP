services:
  celcat-mcp:
    build: .
    container_name: celcat-mcp
    command: ["--http", "--host", "0.0.0.0", "--port", "8001"]
    expose:
      - "8001"
    networks:
      - shared-tunnel
    restart: unless-stopped

networks:
  shared-tunnel:
    external: true
